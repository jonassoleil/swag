name: adv-ml-swa_g
channels:
  - defaults
dependencies:
  - python=3.6  # Google Colab is on Python 3.6
  - pip
  - pip:
    - pip-tools
  - cudatoolkit=10.1 [--no-deps] # 
  - cudnn=7.6 [-- no-deps] # 
